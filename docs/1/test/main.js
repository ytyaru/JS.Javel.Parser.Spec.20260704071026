import { describe, test, expect } from "bun:test";
import {JavelParser} from '../src/main.js';
describe('JavelParser', () => {
    test('defined', ()=>expect(JavelParser).toBeDefined());
    test('', ()=>{

    });
});

